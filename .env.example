# Google Calendar MCP Server Configuration
# ==============================================

# HTTP Server Configuration
# -------------------------
# Port for the HTTP server (default: 8081)
# This should match the port configured in your matey.yaml if using m8e
HTTP_PORT=8081

# Google OAuth Configuration
# -------------------------
# Path to your Google OAuth2 credentials file
# Download this from Google Cloud Console > APIs & Services > Credentials
CALENDAR_OAUTH_PATH=./gcp-oauth.keys.json

# Path where user authentication tokens will be stored
# This file is created automatically after successful authentication
CALENDAR_CREDENTIALS_PATH=~/.calendar-mcp/credentials.json

# Server Configuration
# -------------------
# Time zone for the server (optional, defaults to system timezone)
TZ=America/New_York

# Debug Configuration
# ------------------
# Set to true to enable debug logging
# DEBUG=false

# Google Calendar API Configuration
# --------------------------------
# Maximum number of events to return in list operations (default: 250)
# MAX_RESULTS=250

# Default calendar ID to use when none specified (default: primary)
# DEFAULT_CALENDAR_ID=primary

# OAuth2 Configuration
# --------------------
# OAuth2 redirect URL (should match Google Cloud Console configuration)
# OAUTH_REDIRECT_URL=http://localhost:3000/oauth2callback

# Production Configuration
# ------------------------
# Set to production to disable debug features
# NODE_ENV=development

# Docker Configuration
# -------------------
# When running in Docker, these paths may need adjustment
# DOCKER_CALENDAR_OAUTH_PATH=/home/calendar/.calendar-mcp/gcp-oauth.keys.json
# DOCKER_CALENDAR_CREDENTIALS_PATH=/home/calendar/.calendar-mcp/credentials.json
